# Server Configuration
PORT=8787
HOST=127.0.0.1

# Upstream endpoints
ZAI_UPSTREAM_URL=https://api.z.ai/api/anthropic
ANTHROPIC_UPSTREAM_URL=https://api.anthropic.com

# Z.ai (GLM AI Studio) API Key
# Get your key from https://z.ai
ZAI_API_KEY=YOUR_ZAI_API_KEY

# Optional: Anthropic API Key (sk-ant-...)
# If set, the proxy will ALWAYS use x-api-key for Anthropic and will drop any incoming authorization header.
# This is needed in some environments where `authorization: Bearer ...` is not accepted by api.anthropic.com.
# ANTHROPIC_API_KEY=sk-ant-...

# Optional: Replace Claude Haiku model with GLM
ANTHROPIC_DEFAULT_HAIKU_MODEL=glm-4.7

# Node Environment
NODE_ENV=production
